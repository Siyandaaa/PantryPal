{
  "name": "pantrypal-auth-api",
  "version": "1.0.0",
  "description": "PantryPal's custom authentication API - the one API required by the PROG7314 POE brief. Backed by Firebase (Admin SDK + Identity Toolkit REST + Firestore).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "firebase-admin": "^12.5.0",
    "morgan": "^1.10.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
